# requirements_cpanel.txt
# Clean requirements for Linux/cPanel deployment.
# Generated from pyproject.toml — Windows-only packages removed.
# Install with:  pip install -r requirements_cpanel.txt

# Core framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
starlette>=0.27.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0   # PostgreSQL — use this if your cPanel host has Postgres
# If you only have MySQL/MariaDB on cPanel, replace the line above with:
# PyMySQL>=1.1.0

# Auth / security
passlib[argon2]>=1.7.4
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.6
authlib>=1.3.0
itsdangerous>=2.1.0
cryptography>=41.0.0

# Data / finance
pandas>=2.1.0
numpy>=1.26.0
yfinance>=0.2.0
python-dateutil>=2.9.0
requests>=2.31.0
httpx>=0.25.0

# Broker integrations
kiteconnect>=3.9.9
py5paisa>=0.7.0

# Templating / config
jinja2>=3.0.0
python-dotenv>=1.0.0

# Pydantic
pydantic>=2.0.0
pydantic[email]>=2.0.0

# ASGI→WSGI bridge (required for Passenger)
asgiref>=3.7.0
